# MiDoc — Example Docusaurus Site

This is a small example Docusaurus site generated for demonstration.

Getting started:

```bash
cd example-docusaurus
npm install
npm start
```

Edit docs in `docs/` and blog posts in `blog/`.
